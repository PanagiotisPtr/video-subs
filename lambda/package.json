{
  "name": "video-processing-lambda",
  "version": "1.0.0",
  "description": "Lambda function for video processing trigger",
  "main": "src/index.js",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node dist/index.js"
  },
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.42.0",
    "@aws-sdk/client-sqs": "^3.42.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.88",
    "@types/node": "^16.0.0",
    "typescript": "^4.3.5"
  }
}
